[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5

[deploy.healthcheck]
path = "/"
timeout = 30

[[deploy.services]]
name = "frontend"

[deploy.services.env]
NODE_VERSION = "18"
